# 小狐狸大冒险 · 第一关 开发总结

> 日期：2026-08-03
> 项目路径：`/Users/qinweigh/ai_coding/koodrive/fox-adventure/index.html`
> 试玩方式：`cd fox-adventure && python3 -m http.server 8010`，浏览器打开 http://localhost:8010/index.html （加 `?autoplay` 可跳过标题直接开局）

## 需求背景

想做一个类似超级马里奥的横版平台跳跃小游戏第一关。由于任天堂版权问题，不使用任何原始素材，所有美术资源全部用代码原创绘制，最终部署为 HTML 网页直接可玩。

## 技术方案

- **单文件零依赖**：一个 `index.html` 搞定，HTML + Canvas + 原生 JS，无任何外部图片/库/构建工具
- **像素复古风**：所有素材（角色、地块、敌人、背景）都用 `fillRect` 逐像素块绘制，`imageSmoothingEnabled=false` 保证像素感
- **主角**：原创橙色小狐狸（避开马里奥形象），跑步两帧动画 + 大尾巴摆动 + 跳跃收腿
- **本地服务**：`python3 -m http.server` 拉起即可玩

## 实现的功能

- **关卡元素**：草皮地面、砖块、问号方块（顶出金币）、水管、悬浮石台、4 处坑洞、8 级终点大楼梯、终点旗杆
- **敌人系统**：慢速蜗牛 ×5、快速甲虫 ×4，可踩扁（+100 分），侧面碰撞掉命
- **游戏系统**：3 条命、金币（集满 100 奖 1 命）、分数、300 秒倒计时、受击无敌闪烁、掉坑死亡、抵达旗杆结算时间奖励
- **手感优化**（平台跳跃游戏的关键）：
  - 土狼时间（coyote time）：离开平台边缘后 6 帧内仍可跳
  - 跳跃预输入（jump buffer）：落地前按跳也生效
  - 可变跳高：短按小跳、长按大跳（松开跳跃键时截断上升速度）
- **背景**：云朵和远山双层视差滚动

## 开发过程中的关键点

1. **先确认设计再动手**：用 AskUserQuestion 快速敲定了像素复古风 + 小狐狸主角两个关键决策，然后一页纸设计确认后才开始写码
2. **无头浏览器截图验证**：本机有 Chrome，用 `Google Chrome --headless --screenshot` 对标题页、起点区、中段、终点区分别截图检查渲染，发现并修复了两个视觉问题：
   - 背景远山原是实心绿矩形，太像可站立平台 → 改成阶梯金字塔形 + 黄绿色区分
   - 太阳是黄色方块，容易被误认为游戏方块 → 加了像素光芒
3. **留了 `?autoplay` 调试参数**：`?autoplay=列号` 可以把主角传送到关卡任意位置，方便快速检查各区域渲染，也可用于分享直达链接

## 验证情况

- `node --check` 通过 JS 语法校验
- 截图验证标题、起点、水管区、终点区渲染正常
- 物理手感（碰撞、跳跃、踩敌）为标准 AABB + 瓦片网格实现，代码走查确认，实际手感待玩家反馈

## 后续可迭代方向

- 音效/BGM（Web Audio API 程序生成，保持零素材依赖）
- 更多关卡、移动平台、弹簧、隐藏砖块
- 移动端触屏虚拟按键
- 关卡数据外置为 JSON，支持可视化关卡编辑器
